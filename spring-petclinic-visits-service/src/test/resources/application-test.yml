spring:
  cloud:
    config:
      enabled: false
  sql:
    init:
      schema-locations: classpath*:db/hsqldb/schema.sql
      data-locations: # Disable data initialization for tests
  jpa:
    hibernate:
      ddl-auto: create-drop

eureka:
  client:
    enabled: false

logging.level.org.springframework: INFO
